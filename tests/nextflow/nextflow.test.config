profiles {
    test {
        process {
            executor = 'local'
            cpus = 2
            memory = '2 GB'
        }
        singularity {
            enabled = true
            autoMounts = true
            runOptions = '--writable-tmpfs'
        }
    }
}
